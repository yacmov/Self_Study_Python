animal = "Dog"		        # add Dog into animal
name = "Happy"		        # add Happy into name
hobby = "go to park"        # add walk into hobby
age = 4			            # add 4 into age
is_adult = age >= 3		    # if age is equal and greater shown True else False

print("My " + animal + "'s name is " + name)
print(name + "'s age is  " + str(age)+ ", and love "+ hobby +".")
# Variable(age) is number so need to change type to "str if want to use in the str"
print("Is " + name + " an adult? " + str(is_adult))
# Variable(is_adult) is boolean so need to change type to "str if want to use in the str"